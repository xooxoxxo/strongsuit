# Sponsor button config. GitHub shows the button only for uncommented entries.
# Fill in when the account exists (see RELEASE-PLAN.md, gate G1):
# ko_fi: <ko-fi handle>
# github: <GitHub Sponsors login, once the listing is approved>
